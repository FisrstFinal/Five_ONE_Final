<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bundleDAO">
	<select id="bundleList" parameterType="String" resultType="bundleDTO">
		select bundle_code, bundle_title from bundlelist where channel_code = #{ownerCode} order by bundle_regdate
	</select>
	
	<insert id="bundleAdd" parameterType="bundleDTO">
		insert into bundlelist values (#{bundle_code}, #{bundle_title}, default, default, #{channel_code})
	</insert>
	
	<delete id="bundleDel" parameterType="String">
		delete from bundlelist where bundle_code = #{bundleCode}
	</delete>
	
	<update id="bundleVideoDel" parameterType="String">
		update playlist set playlist_code = '' where playlist_code = #{bundleCode}
	</update>
	
	<insert id="createBundle" parameterType="map">
		insert into bundlelist values (#{bundleCode}, #{bundleName}, default, default, #{ownerCode})
	</insert>
	
	<select id="selectBundle" parameterType="playlistDTO" resultType="bundleDTO">
		select channel_code, bundle_title from bundlelist where bundle_code = #{playlist_code}
	</select>
	
	<insert id="selectAddBundle" parameterType="playlistDTO">
		insert into playlist values (#{channel_code}, #{playlist_title}, #{playlist_code}, #{video_code}, CURRENT_DATE)
	</insert>
</mapper>